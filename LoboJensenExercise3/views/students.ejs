<!DOCTYPE html>

<html>

<head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <title>Student List</title>

</head>

<body>
    <h2>Students</h2>
    <% if(students) { %> <!--To check if the else statement works, make students == false to check when there is not students in the student array-->

        <ul>
            <% students.forEach((students) =>{ %>
                <li><%= students.name %></li>
            <% }) %>
        </ul>

    <% } else {%>
        <p>There is no student to display</p>
    <% } %>

</body>

</html>